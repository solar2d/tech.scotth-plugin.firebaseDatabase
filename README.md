Sources for the plugin `plugin.firebaseDatabase`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.firebaseDatabase" = {
            publisherId = "tech.scotth",
        },
    },
}
```
